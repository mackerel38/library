#include "template"

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout<<fixed<<setprecision(30);
    int T=1;
    // cin >> T;
    while (T--) solve();
}

void solve() {
}